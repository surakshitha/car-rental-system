CREATE TABLE `admin` (
  id int(11) AUTO_INCREMENT,
  email varchar(100),
  password varchar(100) NOT NULL,
  PRIMARY KEY(id, email)
);

INSERT INTO carrental.admin (id, email, password)
VALUES (1, "admin@gmail.com", "admin123");